1. Sum of first N numbers
Parametrized f(n,0) -> main
void f(int i, int sum){
  if(i<1){
    cout<<sum<<endl;
    return;
  }
  f(i-1,sum+i);
}
Functional 
int f(int n){
 if(n==0) return 0;
 return n + f(n-1);
}
2. Factorial 
int print(int n){
 if(n==1) return 1;
 return n* print(n-1);
} Time Complexity: O(N) 

3. Reverse Array
void rev(int l, int r, int arr[]){
    if(l<r){
    swap(arr[l], arr[r]);
    rev(l+1, r-1,arr);} 
} Time Complexity: O(n)
