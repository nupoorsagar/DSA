1. Sum of first N numbers
Parametrized f(n,0) -> main
void f(int i, int sum){
  if(i<1){
    cout<<sum<<endl;
    return;
  }
  f(i-1,sum+i);
}
Functional 
int f(int n){
 if(n==0) return 0;
 return n + f(n-1);
}
